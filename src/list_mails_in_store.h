#ifndef __MAILSYNC_LIST_EMAILS_IN_STORE__

#include "options.h"           // options and default settings
#include "store.h"             // Store

bool list_mails_in_store(options_t* options, Store& store_a);

#endif
